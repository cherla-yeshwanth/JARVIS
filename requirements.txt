# JARVIS v2.0 FREE MASTERPLAN Dependencies
# All free, all offline-capable (except duckduckgo-search which needs internet for searches)

# Speech-to-Text
faster-whisper>=0.10.0

# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# Global Hotkey
keyboard>=0.13.5

# Vector Database (semantic memory)
chromadb>=0.4.0

# Web Search (free, no API key)
duckduckgo-search>=6.0.0

# HTTP client (for Ollama API)
requests>=2.31.0

# Environment config
python-dotenv>=1.0.0

# System monitoring
psutil>=5.9.0

# Backup TTS (Windows SAPI voices)
pyttsx3>=2.90

# Wake Word
openwakeword>=0.6.0
pyaudio>=0.2.13

# Vision + UI Automation
mss>=9.0.1
pyautogui>=0.9.54
pillow>=10.0.0

# Natural Voice TTS
kokoro>=0.9.2
misaki[en]>=0.9.2

# Optional: Custom wake word training
# torch>=2.0.0
# torchaudio>=2.0.0
# speechbrain>=0.5.16
